syntax="proto3";

package mu;

// User profile + last location,
// sent as response in LocationUpdateReply

import "model/MapElementType.proto";

message MapElement {
	// Location
	double lat = 1;
	double lon = 2;

	// Map element
	mu.MapElementType type = 3;
	bytes body = 4;
}

// vim: noet:ts=2
